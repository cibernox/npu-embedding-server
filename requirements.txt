# Pinned to compatible-release ranges. These were unbounded `>=` floors, which
# meant any rebuild silently pulled the newest major — a rebuild today would
# have jumped transformers 4.57 -> 5.x and changed tokenizer behaviour under a
# server whose output vectors must stay byte-stable against a stored index.
fastapi~=0.136.3
uvicorn~=0.48.0
transformers~=4.57.6
numpy~=2.4.6
openvino~=2026.3.1
